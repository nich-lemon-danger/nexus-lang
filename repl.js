const readline = require("readline"); const Lexer = require("./lexer"); const Parser = require("./parser"); const Interpreter = require("./interpreter"); const interpreter = new Interpreter(); const rl = readline.createInterface({ input: process.stdin, output: process.stdout }); console.log("Nexus v1.0 - Type your code below. Ctrl+C to exit."); function prompt() { rl.question("nexus> ", (input) => { if (!input.trim()) { prompt(); return; } try { const tokens = new Lexer(input).tokenize(); const ast = new Parser(tokens).parse(); interpreter.run(ast); } catch(e) { console.log("Error:", e.message); } prompt(); }); } prompt();
