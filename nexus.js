const fs = require("fs"); const Lexer = require("./lexer"); const Parser = require("./parser"); const Interpreter = require("./interpreter"); const file = process.argv[2]; if (!file) { console.log("Usage: node nexus.js <file.nx>"); process.exit(1); } const code = fs.readFileSync(file, "utf8"); const tokens = new Lexer(code).tokenize(); const ast = new Parser(tokens).parse(); const interpreter = new Interpreter(); interpreter.run(ast);
